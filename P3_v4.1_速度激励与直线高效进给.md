# P3（v4）：速度激励与直线段高效进给（统一脚本自验证 + 自动验收 + 人审清单）

> **P3 目标**：解决"全程速度偏低"问题。P1/P2 解决了拐角圆弧化与出弯回线，但直线段速度仍远未达到 $v_{\max}$。  
> P3 通过速度正激励、时间惩罚调优和约束参数校准，使智能体主动追求高速进给。

---

## Step 0：前置闸门（必须）

P2 必须已 PASS：
- `artifacts/p2_accept/summary.json`（`passed=true`）

> ⚠️ 同样防"伪通过"：P3 验收 baseline 必须指向 P2 最佳 PASS 结果。

---

## Step 1：运动学参数（沿用 P1 配置）

> **注意**：运动学参数已在 P1 中统一配置，P3 直接沿用。

### 统一运动学约束

```yaml
kinematic_constraints:
  MAX_VEL: 100.0       # 100 mm/s（即 6000 mm/min）
  MAX_ACC: 2000.0      # 2000 mm/s²
  MAX_JERK: 20000.0    # 20000 mm/s³
  MAX_ANG_VEL: 6.283185307179586   # 2π rad/s
  MAX_ANG_ACC: 100.0   # rad/s²
  MAX_ANG_JERK: 1000.0 # rad/s³
```

| 参数 | 值 | 单位 | 等效 (mm/min) |
|------|-----|------|---------------|
| MAX_VEL | 100 | mm/s | 6000 mm/min |
| MAX_ACC | 2000 | mm/s² | 7,200,000 mm/min² |
| MAX_JERK | 20000 | mm/s³ | 4.32×10⁹ mm/min³ |

---

## Step 2：速度正激励机制

文件：`src/environment/reward.py`

### 2.1 增加速度激励项

当前奖励结构（P0）：
- `r_progress`：进度正激励（主导）
- `r_track`：轮廓误差惩罚
- `r_dir`：航向误差惩罚
- `r_time`：时间惩罚

**新增**：
```python
# P3: 速度正激励
v_ratio = v_exec / max(v_max, 1e-6)
r_speed = + w_v * v_ratio  # 正激励，w_v 建议 2.0~5.0
```

### 2.2 调整权重平衡

| 权重 | P0 值 | P3 建议值 | 说明 |
|------|-------|-----------|------|
| w_s | 20.0 | 20.0~25.0 | 保持进度主导 |
| w_e | 5.0 | 3.0~5.0 | 可适当降低 |
| w_t | 1.0 | 0.5~1.5 | 时间惩罚适中 |
| **w_v** | 0.0 | **3.0~5.0** | **新增速度激励** |

---

## Step 3：直线段识别与差异化策略

文件：`src/environment/cnc_env.py`

### 3.1 直线段判定

```python
# 直线段定义：前方一定距离内无显著曲率
is_straight = dist_to_turn > d_straight_threshold and abs(kappa) < kappa_straight_eps
```

参数建议：
- `d_straight_threshold`：至少 3× lookahead_spacing
- `kappa_straight_eps`：0.01~0.05 rad/mm

### 3.2 直线段速度目标上调

```python
if is_straight:
    speed_target = v_max * 0.95  # 直线段目标接近最大
else:
    speed_target = v_target_turn_aware  # 沿用 P4.0 的 turn-aware 目标
```

---

## Step 4：时间惩罚与进度激励联动

### 4.1 时间惩罚调优

当前 `p4.time_penalty = -0.01`，可能过小。

建议：
- 若希望更积极追速：`time_penalty = -0.05 ~ -0.1`
- 配合速度激励避免"低速等进度"的局部最优

### 4.2 进度倍增器（可选）

在直线段可以给予更高的 `progress_multiplier`：
```python
progress_multiplier = 1.2 if is_straight else 1.0
r_progress = w_s * progress_diff * progress_multiplier
```

---

# ✅ P3 自验证（smoke）：速度响应测试

## A) 目标
- 验证速度激励生效：恒定动作下速度应接近 v_max
- 验证直线段速度目标正确识别

## B) 命令
```powershell
cd PPO_project
python tools/acceptance_suite.py --phase p3_smoke --config configs/train_square_p3.yaml --episodes 3 --out artifacts/p3_smoke
```

产物：`artifacts/p3_smoke/trace.csv`

## C) 关键检查项
1. 直线段平均速度 / v_max ≥ 0.8
2. 速度激励项 `r_speed` 为正
3. 无 NaN/Inf

---

# ✅ P3 自动化验收（训练后，带 P2 baseline 对比）

训练：
```powershell
python main.py --mode train --config configs/train_square_p3.yaml
```

验收：
```powershell
python tools/acceptance_suite.py --phase p3_eval --config configs/train_square_p3.yaml --model <PATH_TO_best_model.pth> --episodes 50 --baseline artifacts/p2_accept/summary.json --out artifacts/p3_accept --deterministic
```

**自动 PASS 条件（脚本内）**
- 继承 P2：success / stall / progress / error 不退化
- 直线段平均速度 ≥ 0.85 × v_max
- 全程平均速度较 P2 提升 ≥ 20%
- 完成时间较 P2 缩短 ≥ 15%

---

# 👀 P3 必要的人为验收清单（必要时）

1) 直线段速度曲线稳定在高位，无大幅波动
2) 拐角处仍保持圆弧过渡（不因速度激励破坏 P1 成果）
3) 轮廓误差仍在容许带内（不因追速超差）
4) Reward 分解：`r_speed` 贡献显著但不压过 `r_progress`

---

# 附录：P2 扩展建议

如果不希望新增 P3，可以在 P2 中追加以下内容：

## P2.x：速度软约束增强

在 `Step 2: 速度软约束` 基础上增加：

### 2.x.1 速度正激励
- `r_speed = + w_v * (v_exec / v_max)`
- 权重 `w_v = 3.0`（可在 YAML 中配置）

### 2.x.2 直线段速度目标上调
- 识别直线段（`dist_to_turn > threshold`）
- 直线段 `speed_target = 0.95 * v_max`

### 2.x.3 验收条件追加
- 直线段平均速度 ≥ 0.85 × v_max
- 全程平均速度较 P1 baseline 提升 ≥ 15%

---

# 🚀 P3 自动化执行脚本

**执行入口**（保存为 `PPO_project/tools/run_p3_pipeline.ps1`）：

```powershell
# P3 完整流水线：Smoke → Train → Eval
param([int]$SmokeEpisodes = 3, [int]$EvalEpisodes = 50, [switch]$SkipSmoke, [switch]$SkipTrain)

$ErrorActionPreference = "Stop"
Set-Location $PSScriptRoot\..

# 前置检查：P2 必须 PASS
$p2 = Get-Content artifacts/p2_accept/summary.json | ConvertFrom-Json
if (-not $p2.passed) { Write-Error "P2 not passed"; exit 1 }

if (-not $SkipSmoke) {
    Write-Host "=== P3 Smoke ===" -ForegroundColor Cyan
    python tools/acceptance_suite.py --phase p3_smoke --config configs/train_square_p3.yaml --episodes $SmokeEpisodes --out artifacts/p3_smoke
}

if (-not $SkipTrain) {
    Write-Host "=== P3 Train ===" -ForegroundColor Cyan
    python main.py --mode train --config configs/train_square_p3.yaml
}

Write-Host "=== P3 Eval ===" -ForegroundColor Cyan
$latestDir = Get-ChildItem saved_models/train -Directory | Sort-Object Name -Descending | Select-Object -First 1
python tools/acceptance_suite.py --phase p3_eval --config configs/train_square_p3.yaml --model "$($latestDir.FullName)/checkpoints/best_model.pth" --episodes $EvalEpisodes --baseline artifacts/p2_accept/summary.json --out artifacts/p3_accept --deterministic
$eval = Get-Content artifacts/p3_accept/summary.json | ConvertFrom-Json
if ($eval.passed) { Write-Host "P3 PASSED" -ForegroundColor Green } else { Write-Error "P3 FAILED" }
```

**运行**：`powershell -NoProfile -ExecutionPolicy Bypass -File PPO_project/tools/run_p3_pipeline.ps1`
