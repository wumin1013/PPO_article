# Phase A2：吞吐加速（可并行）（FINAL v1.7.2）
版本日期：2025-12-30  

定位：**低频任务**。用于提升吞吐（更快做更多 seed/episodes），**只做代码层面的训练/验收提速，不改变算法语义与默认功能**。可与 Phase B 并行推进，但不能破坏 A1 的可追溯性。

## A-2（造机器）：吞吐加速（可并行）
定位：不改变学习问题定义，只减少你等实验结果的时间。

典型手段（实现方式不限，**仅限代码层面提速**）：
- 训练提速：减少不必要的 IO（日志/图像/轨迹写入频率），降低可视化开销，合理设置保存周期
- 环境提速：向量化/批量 rollout（不改变状态/奖励/动力学语义）
- eval 提速：分级评测（smoke/fast/full），只在关键 checkpoint 上做 full eval
- 并行策略：多进程/多卡并行不同 seed（保证每个 run 的 manifest 独立、可追溯）

执行时序（本次主线）：
- **A0 完成后立刻执行 A2**（只需本次 P0 验收后跑一次）
- 后续 PhaseB 的每个 run **不需要重复执行 A0/A2**，只需按 A1 标准落盘

验收：
- 不改变默认训练行为
- 不引入新的评测口径
- 仍以 A-1 作为落盘验收标准（P0_L2 回归不退化）

---
