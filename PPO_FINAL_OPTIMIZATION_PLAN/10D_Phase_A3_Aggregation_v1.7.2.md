# Phase A3：聚合与论文主表流水线（可并行）（FINAL v1.7.2）
版本日期：2025-12-30  

定位：**低频任务**。建立自动化汇总与论文主表聚合规则，确保你后续绘图/表格生成不靠手工“拼凑”。可与 Phase B 并行推进，但输出必须引用 A1 的 Run Bundle。

## A-3（造流水线）：多 seed 自动化 + 论文主表聚合（可并行）
目标：从 Run Bundle 自动生成：
- 论文主表（每个 run 的核心指标）
- 消融表（开关组合 → 指标变化）
- 绘图所需的曲线数据（来自 trace 或 summary 聚合）

验收：
- 聚合只读取 Run Bundle（不读临时训练目录）
- 同一批 Run Bundle 可重复生成相同表格（可审计、可复现）

---
